{
  "_comment": "File naming: {scope}-{service}-{env}.json — see ADR-0011 for schema reference",
  "_redaction_note": "All credential values MUST use <REDACTED:{type}> in committed files. Types: password, token, ssh-pubkey, ssh-privkey, secret, ip-range",
  "vault_path": "secret/{scope}/{service}/{env}",
  "description": "[Human-readable purpose — what this credential is for]",
  "access": "rw|ro",
  "owner": "rune|yboujraf",
  "target": "[Device — IP:port or service URL]",
  "fields": {
    "host": "<REDACTED:ip-range>",
    "port": 0,
    "username": "<REDACTED:secret>",
    "password": "<REDACTED:password>"
  }
}
